name: CI
on: [pull_request]

env:
  JAVA_VERSION: 19
  JAVA_DIST: adopt

jobs:
  instrumented-tests:
    name: Run Instrumented Tests | API ${{ matrix.api-level }} | JDK ${{ env.JAVA_VERSION }}
    runs-on: macos-latest
    strategy:
      matrix:
        api-level: [ 31 ]

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v2
        with:
          distribution: ${{ env.JAVA_DIST }}
          java-version: ${{ env.JAVA_VERSION }}

      - name: Run All Android Tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ matrix.api-level }}
          arch: x86_64
          profile: Nexus 6
          script: ./gradlew connectedCheck --info

  unit-tests:
    name: Run Unit Tests | JDK ${{ env.JAVA_VERSION }}
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v2
        with:
          distribution: ${{ env.JAVA_DIST }}
          java-version: ${{ env.JAVA_VERSION }}

      - name: Run All Unit Tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 31
          arch: x86_64
          profile: Nexus 6
          script: ./gradlew test --info
